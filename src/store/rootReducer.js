import { combineReducers } from 'redux';

import senderMessage from 'store/senderMessage/reducer';
// import senderMessage from 'store/senderMessage/reducer';

export default combineReducers({
  senderMessage,
});
